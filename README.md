
# 🚀 VocabMaster — Руководство по Запуску (Production Guide)

**VocabMaster** — это Telegram Mini App для изучения английского языка с ИИ.

---

## ⚙️ Настройка AI (DeepSeek API)

Приложение настроено по умолчанию на использование **DeepSeek API**, так как это наиболее выгодное и качественное решение для русского языка.

### 1. Получение ключа DeepSeek
1. Зарегистрируйтесь на [platform.deepseek.com](https://platform.deepseek.com/).
2. Создайте API Key в разделе "API Keys".
3. Убедитесь, что на балансе есть средства (хотя бы 1-2 доллара).

### 2. Настройка переменных окружения
Создайте файл `.env.local` в корне проекта и добавьте следующие строки:

```env
# Ваш DeepSeek API ключ
VITE_AI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Адрес API (Оставьте пустым для дефолтного DeepSeek или укажите свой прокси)
VITE_AI_BASE_URL=https://api.deepseek.com

# Модель (рекомендуется deepseek-chat для JSON режима)
VITE_AI_MODEL=deepseek-chat
```

> **Важно:** После изменения `.env.local` обязательно перезапустите локальный сервер (`npm run dev`).

---

## ✅ Чек-лист перед запуском в Telegram

### 1. Развертывание (Deploy) на Vercel
1. Зарегистрируйтесь на [vercel.com](https://vercel.com).
2. Импортируйте проект.
3. В настройках **Environment Variables** добавьте те же переменные, что и в `.env.local`:
   - `VITE_AI_API_KEY`
   - `VITE_AI_BASE_URL` (по умолчанию `https://api.deepseek.com`)
   - `VITE_AI_MODEL` (по умолчанию `deepseek-chat`)

### 2. Настройка Telegram Бота
1. Откройте **@BotFather** в Telegram.
2. Создайте бота (`/newbot`) или выберите существующего.
3. Введите команду `/newapp`.
4. Введите название и описание.
5. Вставьте ссылку на ваше приложение из Vercel (например, `https://vocab-master.vercel.app`).
6. Получите ссылку вида `t.me/mybot/app`.

---

## 🛠 Техническая информация

### Структура Данных
Слова разбиты на уровни (`src/data/levels/`) и загружаются лениво.

### Защита от сбоев
В приложение встроен **ErrorBoundary**. Если произойдет критическая ошибка, пользователь увидит экран с кнопкой "Сбросить данные".

### Миграция данных
Приложение использует адаптер (`src/services/storage/core.ts`), который автоматически выбирает между `localStorage` (для браузера) и `Telegram CloudStorage` (для реального приложения).
