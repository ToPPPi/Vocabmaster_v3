
# 🧪 VocabMaster — План Тестирования (Quality Assurance)

Этот документ описывает критические сценарии тестирования (Test Cases) для проверки стабильности приложения перед релизом.

---

## 🛑 Приоритет 1: Критические Пути (Happy Path & Limits)

Эти функции должны работать идеально, так как они составляют основу приложения.

### 1. Ежедневная сессия (Daily Session)
**Цель:** Проверить, что пользователь получает новые слова, учит их, и сессия корректно завершается.

| ID | Сценарий | Шаги | Ожидаемый результат |
| :--- | :--- | :--- | :--- |
| **DS-01** | **Старт сессии (Free)** | 1. Зайти с нового аккаунта (Free).<br>2. Нажать "Начать сессию".<br>3. Выбрать уровень A1. | Открывается карточка первого слова. Кнопки оценки (Сложно/Норм/Легко) активны. |
| **DS-02** | **Оценка слова** | 1. Нажать "Легко" на слове. | Карточка перелистывается на следующее слово. Прогресс-бар сверху увеличивается. Слово добавляется в словарь со статусом `mastered`. |
| **DS-03** | **ИИ-помощник (Лимит)** | 1. Нажать "AI Учитель".<br>2. Повторить 5 раз (для Free).<br>3. Нажать 6-й раз. | Первые 5 раз выдается контент. На 6-й раз кнопка блокируется или выдает сообщение "Лимит исчерпан". |
| **DS-04** | **Завершение (Лимит 10 слов)** | 1. Пройти 10 слов. | Появляется экран "Сессия завершена". После выхода на Дашборд, виджет "План на день" показывает 10/10 и таймер (Закрыто до завтра). |
| **DS-05** | **Блокировка повторного входа** | 1. После завершения сессии (DS-04) попробовать нажать "Начать сессию" снова. | Приложение не дает выбрать уровень для обучения или показывает таймер разблокировки. |

### 2. Система Повторений (Review Session)
**Цель:** Проверить, что алгоритм SRS правильно возвращает слова.

| ID | Сценарий | Шаги | Ожидаемый результат |
| :--- | :--- | :--- | :--- |
| **RS-01** | **Нет слов для повторения** | 1. Создать новый аккаунт.<br>2. Сразу нажать "Повторение". | Экран "Нет слов". Сообщение "Отличная работа, все повторено". |
| **RS-02** | **Появление слова (Hard)** | 1. В Daily Session отметить слово как "Сложно" (Brain).<br>2. Выйти в Дашборд. | Счетчик "Повторение" должен стать `1`. |
| **RS-03** | **Прохождение повторения** | 1. Нажать "Повторение".<br>2. Увидеть слово, отмеченное ранее.<br>3. Нажать "Нормально". | Слово исчезает из очереди. Счетчик на Дашборде становится `0`. |
| **RS-04** | **Отложенное повторение** | 1. Отметить слово "Легко" (Easy). | Слово НЕ появляется в "Повторении" сразу (так как интервал > 1 дня). |

---

## ⚠️ Приоритет 2: Edge Cases (Граничные случаи)

Проверка нестандартного поведения пользователя.

### 3. Управление Аккаунтом и Состоянием
| ID | Сценарий | Шаги | Ожидаемый результат |
| :--- | :--- | :--- | :--- |
| **EC-01** | **Прерывание сессии** | 1. Начать сессию (пройти 3 слова).<br>2. Закрыть приложение/вкладку.<br>3. Открыть снова. | Прогресс 3 слов сохранен в словаре. Дневной лимит показывает 3/10. Можно продолжить. |
| **EC-02** | **Upgrade во время сессии** | 1. Пройти 9 слов (Free).<br>2. Выйти, купить Premium.<br>3. Вернуться в сессию. | Лимит меняется с 10 на Бесконечность. Сессия не прерывается после 10-го слова. |
| **EC-03** | **Смена даты (Reset)** | 1. Имитировать смену даты (можно вручную в БД `localStorage` поменять `lastLoginDate`). | Счетчик `wordsLearnedToday` сбрасывается в 0. Счетчик `aiGenerationsToday` сбрасывается в 0. Таймер исчезает. |
| **EC-04** | **Удаление слова** | 1. Зайти в Словарь.<br>2. Удалить слово, которое было "Сложно". | Слово пропадает из словаря. Слово пропадает из очереди "Повторение". Слово снова доступно в "Уровнях" для изучения. |

### 4. Оплата и Премиум
| ID | Сценарий | Шаги | Ожидаемый результат |
| :--- | :--- | :--- | :--- |
| **PAY-01** | **Визуализация Free** | 1. Проверить профиль Free юзера. | Блоки "Лимит", "Уровни" показывают ограничения. Уровни B1-C2 закрыты замком. |
| **PAY-02** | **Активация Premium** | 1. Нажать "Обновить до Premium". | Появляется корона. Баннер на дашборде исчезает. Уровни B1-C2 открываются. Лимит слов становится ∞. |

---

## 📝 Чек-лист перед релизом
- [ ] Все тексты на русском языке корректны (нет опечаток).
- [ ] Анимации не вызывают лагов на мобильном устройстве.
- [ ] Приложение корректно масштабируется на маленьких экранах (iPhone SE).
- [ ] API ключи OpenAI защищены или работают через прокси (в данном случае через `agentrouter`).
